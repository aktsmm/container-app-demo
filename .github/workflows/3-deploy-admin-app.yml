name: 3️⃣ Deploy Admin App (Container Apps)

on:
  workflow_dispatch:
    inputs:
      imageTag:
        description: "ACR にプッシュ済みの admin-app イメージタグ (空なら latest)"
        required: false
  workflow_run:
    workflows:
      - "2️⃣ Build Admin App"
    types:
      - completed

permissions:
  contents: read
  id-token: write

env:
  RESOURCE_GROUP_NAME: ${{ vars.RESOURCE_GROUP_NAME }}
  ACA_ENVIRONMENT_NAME: ${{ vars.ACA_ENVIRONMENT_NAME }}
  CONTAINER_APP_NAME: ${{ vars.ADMIN_CONTAINER_APP_NAME }}
  ACR_NAME_PREFIX: ${{ vars.ACR_NAME_PREFIX }}
  STORAGE_ACCOUNT_PREFIX: ${{ vars.STORAGE_ACCOUNT_PREFIX }}
  ADMIN_IMAGE_NAME: admin-app
  ADMIN_TARGET_PORT: 8000
  DB_ENDPOINT: ${{ vars.DB_ENDPOINT }}
  BACKUP_CONTAINER: ${{ vars.BACKUP_CONTAINER_NAME }}
  DB_APP_USERNAME: ${{ vars.DB_APP_USERNAME }}
  DB_APP_PASSWORD: ${{ vars.DB_APP_PASSWORD }}
  ACA_ADMIN_USERNAME: ${{ vars.ACA_ADMIN_USERNAME }}
  ACA_ADMIN_PASSWORD: ${{ vars.ACA_ADMIN_PASSWORD }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: コードを取得
        uses: actions/checkout@v4

      - name: Azure に Service Principal でログイン
        uses: azure/login@v2
        with:
          creds: >-
            {"clientId":"${{ vars.AZURE_CLIENT_ID }}","tenantId":"${{ vars.AZURE_TENANT_ID }}","clientSecret":"${{ vars.AZURE_CLIENT_SECRET }}","subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}"}

      - name: ACR 名を解決
        run: |
          set -euo pipefail
          if [ -z "$ACR_NAME_PREFIX" ] || [ -z "$RESOURCE_GROUP_NAME" ]; then
            echo "ACR 名を決定するための変数が不足しています" >&2
            exit 1
          fi
          ACR_NAME=$(az acr list \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --query "[?starts_with(name, '${ACR_NAME_PREFIX}')].name | [0]" \
            -o tsv)
          if [ -z "$ACR_NAME" ]; then
            echo "指定プレフィックスの ACR が存在しません。infra-deploy を先に実行してください" >&2
            exit 1
          fi
          echo "ACR_NAME=$ACR_NAME" >> "$GITHUB_ENV"
          echo "ACR_LOGIN_SERVER=${ACR_NAME}.azurecr.io" >> "$GITHUB_ENV"

      - name: Storage Account 名を解決
        run: |
          set -euo pipefail
          STORAGE_NAME=$(az storage account list \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --query "[?starts_with(name, '${STORAGE_ACCOUNT_PREFIX}')].name | [0]" \
            -o tsv)
          if [ -z "$STORAGE_NAME" ]; then
            echo "指定プレフィックスの Storage Account が存在しません" >&2
            exit 1
          fi
          echo "STORAGE_ACCOUNT_NAME=$STORAGE_NAME" >> "$GITHUB_ENV"

      - name: Container Apps 拡張機能を更新
        run: |
          az extension add --name containerapp --upgrade

      - name: Container Apps Environment 名を動的解決
        run: |
          set -euo pipefail
          # RG 内の Container Apps Environment を検索
          ACTUAL_ENV_NAME=$(az containerapp env list \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --query "[0].name" \
            -o tsv)
          if [ -z "$ACTUAL_ENV_NAME" ]; then
            echo "Container Apps Environment が見つかりません。infra-deploy を先に実行してください" >&2
            exit 1
          fi
          echo "検出された Environment: $ACTUAL_ENV_NAME"
          echo "ACA_ENVIRONMENT_NAME=$ACTUAL_ENV_NAME" >> "$GITHUB_ENV"

      - name: Container Apps Environment のプロビジョニング完了を待機
        run: |
          set -euo pipefail
          MAX_RETRY=20
          INTERVAL=30
          for attempt in $(seq 1 $MAX_RETRY); do
            STATE=$(az containerapp env show \
              --name "$ACA_ENVIRONMENT_NAME" \
              --resource-group "$RESOURCE_GROUP_NAME" \
              --query "properties.provisioningState" -o tsv || echo "Unknown")
            if [ "$STATE" = "Succeeded" ]; then
              echo "Container Apps Environment は正常にプロビジョニング済みです"
              exit 0
            fi
            echo "[待機] 状態: $STATE (試行 $attempt/$MAX_RETRY)"
            sleep $INTERVAL
          done
          echo "Container Apps Environment が規定時間内に Succeeded になりませんでした" >&2
          exit 1

      - name: ACR 管理者認証を有効化
        run: |
          az acr update --name "$ACR_NAME" --admin-enabled true

      - name: イメージタグを決定
        id: image_meta
        run: |
          EVENT_NAME='${{ github.event_name }}'
          INPUT_TAG='${{ github.event.inputs.imageTag }}'

          # 入力タグが指定されている場合はそれを使用
          if [ -n "$INPUT_TAG" ]; then
            IMAGE_TAG="$INPUT_TAG"
            echo "指定されたタグを使用: $IMAGE_TAG"
          else
            # ACR から最新のイメージタグを取得（latest 以外）
            LATEST_TAG=$(az acr repository show-tags \
              --name "$ACR_NAME" \
              --repository "$ADMIN_IMAGE_NAME" \
              --orderby time_desc \
              --top 5 \
              -o tsv | grep -v '^latest$' | head -n 1)
            if [ -n "$LATEST_TAG" ]; then
              IMAGE_TAG="$LATEST_TAG"
              echo "ACR から最新タグを取得: $IMAGE_TAG"
            else
              IMAGE_TAG='latest'
              echo "ACR にタグが見つからないため latest を使用"
            fi
          fi

          echo "IMAGE_TAG=$IMAGE_TAG" >> "$GITHUB_ENV"
          echo "IMAGE_FULL=$ACR_LOGIN_SERVER/$ADMIN_IMAGE_NAME:$IMAGE_TAG" >> "$GITHUB_ENV"

      - name: ACR Pull 用認証情報を取得
        id: acr_token
        run: |
          ACR_USERNAME=$(az acr credential show --name "$ACR_NAME" --query username -o tsv)
          ACR_PASSWORD=$(az acr credential show --name "$ACR_NAME" --query "passwords[0].value" -o tsv)
          echo "ACR_USERNAME=$ACR_USERNAME" >> "$GITHUB_ENV"
          echo "ACR_PASSWORD=$ACR_PASSWORD" >> "$GITHUB_ENV"

      - name: MySQL VM の IP アドレスを取得
        run: |
          VM_IP=$(az vm list-ip-addresses \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --name "${{ vars.VM_NAME }}" \
            --query "[0].virtualMachine.network.privateIpAddresses[0]" -o tsv)
          echo "DB_ENDPOINT_RESOLVED=${VM_IP}:3306" >> "$GITHUB_ENV"
          echo "MySQL VM IP: $VM_IP"

      - name: Container Apps へデプロイ
        run: |
          # Container App が存在するか確認
          if az containerapp show --name "$CONTAINER_APP_NAME" --resource-group "$RESOURCE_GROUP_NAME" &>/dev/null; then
            echo "既存の Container App を更新します"
            
            # レジストリ認証情報を設定
            az containerapp registry set \
              --name "$CONTAINER_APP_NAME" \
              --resource-group "$RESOURCE_GROUP_NAME" \
              --server "$ACR_LOGIN_SERVER" \
              --username "$ACR_USERNAME" \
              --password "$ACR_PASSWORD"
            
            # シークレットを更新
            az containerapp secret set \
              --name "$CONTAINER_APP_NAME" \
              --resource-group "$RESOURCE_GROUP_NAME" \
              --secrets admin-username="$ACA_ADMIN_USERNAME" admin-password="$ACA_ADMIN_PASSWORD"
            
            # Container App を更新
            az containerapp update \
              --name "$CONTAINER_APP_NAME" \
              --resource-group "$RESOURCE_GROUP_NAME" \
              --image "$IMAGE_FULL" \
              --revision-suffix "gh-${GITHUB_RUN_NUMBER}" \
              --set-env-vars \
                ADMIN_USERNAME=secretref:admin-username \
                ADMIN_PASSWORD=secretref:admin-password \
                DB_ENDPOINT="$DB_ENDPOINT_RESOLVED" \
                DB_APP_USERNAME="$DB_APP_USERNAME" \
                DB_APP_PASSWORD="$DB_APP_PASSWORD" \
                DB_NAME="boardapp" \
                BACKUP_CONTAINER="$BACKUP_CONTAINER" \
                STORAGE_ACCOUNT_NAME="$STORAGE_ACCOUNT_NAME"
          else
            echo "新規 Container App を作成します"
            
            # Container App を作成
            az containerapp create \
              --name "$CONTAINER_APP_NAME" \
              --resource-group "$RESOURCE_GROUP_NAME" \
              --environment "$ACA_ENVIRONMENT_NAME" \
              --image "$IMAGE_FULL" \
              --ingress external \
              --target-port "$ADMIN_TARGET_PORT" \
              --min-replicas 0 \
              --max-replicas 1 \
              --registry-server "$ACR_LOGIN_SERVER" \
              --registry-username "$ACR_USERNAME" \
              --registry-password "$ACR_PASSWORD" \
              --revision-suffix "gh-${GITHUB_RUN_NUMBER}"
            
            # シークレットを設定
            az containerapp secret set \
              --name "$CONTAINER_APP_NAME" \
              --resource-group "$RESOURCE_GROUP_NAME" \
              --secrets admin-username="$ACA_ADMIN_USERNAME" admin-password="$ACA_ADMIN_PASSWORD"
            
            # 環境変数を追加（シークレット参照含む）
            az containerapp update \
              --name "$CONTAINER_APP_NAME" \
              --resource-group "$RESOURCE_GROUP_NAME" \
              --set-env-vars \
                ADMIN_USERNAME=secretref:admin-username \
                ADMIN_PASSWORD=secretref:admin-password \
                DB_ENDPOINT="$DB_ENDPOINT_RESOLVED" \
                DB_APP_USERNAME="$DB_APP_USERNAME" \
                DB_APP_PASSWORD="$DB_APP_PASSWORD" \
                DB_NAME="boardapp" \
                BACKUP_CONTAINER="$BACKUP_CONTAINER" \
                STORAGE_ACCOUNT_NAME="$STORAGE_ACCOUNT_NAME"
          fi

          # シングルリビジョンモードに設定
          az containerapp revision set-mode \
            --name "$CONTAINER_APP_NAME" \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --mode single

      - name: Container App に Managed Identity を付与
        run: |
          set -euo pipefail
          echo "Container App に System Assigned Managed Identity を有効化します"

          # Managed Identity を有効化
          az containerapp identity assign \
            --name "$CONTAINER_APP_NAME" \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --system-assigned

          # Managed Identity の Principal ID を取得
          PRINCIPAL_ID=$(az containerapp identity show \
            --name "$CONTAINER_APP_NAME" \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --query principalId -o tsv)

          echo "Managed Identity Principal ID: $PRINCIPAL_ID"

          # ⚠️ デモ用：意図的に過剰な権限を付与（本番環境では非推奨）
          # Subscription スコープで Contributor ロールを付与
          echo "⚠️ 【デモ用】Subscription スコープで Contributor ロールを付与します（過剰な権限）"
          az role assignment create \
            --assignee "$PRINCIPAL_ID" \
            --role "Contributor" \
            --scope "/subscriptions/${{ secrets.AZURE_SUBSCRIPTION_ID }}"

          # Storage Account への Storage Blob Data Contributor ロールも付与（冗長だが明示的に）
          STORAGE_ID=$(az storage account show \
            --name "$STORAGE_ACCOUNT_NAME" \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --query id -o tsv)

          echo "Storage Account に対して Storage Blob Data Contributor ロールも付与します"
          az role assignment create \
            --assignee "$PRINCIPAL_ID" \
            --role "Storage Blob Data Contributor" \
            --scope "$STORAGE_ID" || echo "既に割り当て済みまたはエラー（継続）"

          echo "⚠️ 【警告】Managed Identity に Subscription レベルの Contributor が付与されました"
          echo "本番環境では最小権限の原則に従い、必要最小限のロールのみを付与してください"

      - name: デプロイサマリを出力
        if: success()
        run: |
          FQDN=$(az containerapp show \
            --name "$CONTAINER_APP_NAME" \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --query "properties.configuration.ingress.fqdn" -o tsv)

          PROVISIONING_STATE=$(az containerapp show \
            --name "$CONTAINER_APP_NAME" \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --query "properties.provisioningState" -o tsv)

          RUNNING_STATUS=$(az containerapp show \
            --name "$CONTAINER_APP_NAME" \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --query "properties.runningStatus" -o tsv)

          echo "## 🎯 管理アプリ Container Apps デプロイ完了" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🚀 デプロイ情報" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Container App 名**: \`$CONTAINER_APP_NAME\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment**: \`$ACA_ENVIRONMENT_NAME\`" >> $GITHUB_STEP_SUMMARY
          echo "- **イメージ**: \`$IMAGE_FULL\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Provisioning State**: \`$PROVISIONING_STATE\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Running Status**: \`$RUNNING_STATUS\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### 📍 アクセス情報" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **FQDN**: \`$FQDN\`" >> $GITHUB_STEP_SUMMARY
          echo "- **アクセスURL**: https://$FQDN" >> $GITHUB_STEP_SUMMARY
          # --- マスク回避用ワークアラウンド (GitHub Actions の自動レダクションが発生する場合) ---
          # GitHub の秘密値マスク機能は完全一致した文字列を置換するため、分割表示で参照可能性を保持する。
          # クリック可能な完全 URL がマスクされた際に、下記分割を用いて再構築できるようにする。
          FQDN_PART1="${FQDN%%.*}"          # 先頭ラベル
          FQDN_REST="${FQDN#*.}"            # 残り部分
          echo "- **FQDN(分割)**: \`$FQDN_PART1\`.\`$FQDN_REST\`" >> $GITHUB_STEP_SUMMARY
          echo "- **再構築コマンド例**: \`echo $FQDN_PART1.$FQDN_REST\`" >> $GITHUB_STEP_SUMMARY
          # ゼロ幅スペース等は URL クリック性を損なうため使用しない方針。
          echo "- **認証**: Basic 認証 (ID/Password 必要)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### ⚙️ 環境変数" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **DB_ENDPOINT**: \`$DB_ENDPOINT\`" >> $GITHUB_STEP_SUMMARY
          echo "- **BACKUP_CONTAINER**: \`$BACKUP_CONTAINER\`" >> $GITHUB_STEP_SUMMARY
          echo "- **ADMIN_USERNAME**: secretref" >> $GITHUB_STEP_SUMMARY
          echo "- **ADMIN_PASSWORD**: secretref" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "✅ デプロイ日時: $(date +'%Y-%m-%d %H:%M:%S %Z')" >> $GITHUB_STEP_SUMMARY

      - name: FQDN を表示
        run: |
          az containerapp show \
            --name "$CONTAINER_APP_NAME" \
            --resource-group "$RESOURCE_GROUP_NAME" \
            --query "properties.configuration.ingress.fqdn"
